import 'package:flutter/material.dart';

class ConformationNumberScreen extends StatelessWidget {
  const ConformationNumberScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Conformation Number Screen"),
      ),
    );
  }
}